package com.PartieA;

import java.util.regex.Pattern;

public class prog {
    public static void main(String[] args) {
        /*
        Algo_partieA pa = new Algo_partieA();
        String PathFile= "E:\\study\\2021\\data mining\\Projet datamining 2020-2021\\Thyroid_Dataset.txt";

        System.out.println(pa.Simplediscretisation(PathFile,5,4));

        String support ="30";
        String confidence ="40";

        StringBuilder freqItemSets = new StringBuilder("");
        StringBuilder associationRules=new StringBuilder("");
        if(Pattern.matches("[a-zA-Z]+", support) || Pattern.matches("[a-zA-Z]+", confidence)){
            throw new SecurityException("the confidance and the support most be a numerical value");
        }
        if( Integer.parseInt(support)<=100 && Integer.parseInt(support)>=0 && Integer.parseInt(confidence)<=100 && Integer.parseInt(confidence)>=0 ){
            pa = new Algo_partieA();
            pa.Algo_apriori(PathFile,support,confidence,freqItemSets,associationRules);
            String out="";
            out  = out +"\n"+freqItemSets+"\n" +associationRules;
        //System.out.println(out);
        }*/
    }
}
